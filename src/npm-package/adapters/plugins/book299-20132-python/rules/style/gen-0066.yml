---
id: GEN-0066
name: 显式的相对 import 也是一种可以接受的方式，标准库代码应当一直使用绝对 import，避免复杂的包布局。
severity: warning
tool: auto
category: wc-style
description: |
  显式的相对 import 也是一种可以接受的方式，标准库代码应当一直使用绝对 import，避免复杂的包布局。

  Auto-extracted from book299-20132-Python语言编码规范.txt.

check:
  type: ast-grep
  pattern: TODO
