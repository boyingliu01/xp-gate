---
id: GEN-0061
name: 使用 import 开头的导入，一行只能导入一个库，禁止使用逗号分开， 导入多个库。
severity: error
tool: auto
category: wc-style
description: |
  使用 import 开头的导入，一行只能导入一个库，禁止使用逗号分开， 导入多个库。

  Auto-extracted from book299-20132-Python语言编码规范.txt.

check:
  type: ast-grep
  pattern: TODO
